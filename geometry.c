#include <math.h>

#include "geometry.h"

typedef struct{
    int x;
    int y;
} point;

int verify(point p, point a, point b) {
    

    return 0;
}

int inside(point p, point poly[], int n) {
    return 0;
}
